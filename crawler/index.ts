import getCardsInfo from './src/getCardInfo';
import insertCards from './src/insertCards'
import { BASE_URL, PAGE_LIMIT } from './settings';

//getCardsInfo(BASE_URL, PAGE_LIMIT);
insertCards(BASE_URL, PAGE_LIMIT)
